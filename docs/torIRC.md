# torIRC.py

make sure to run the tor server.
Location: `/opt/homebrew/etc/tor/`
Run the tor server in the background with `tor -f /opt/homebrew/etc/tor/torrc.sample &`

Client is having trouble connecting to the server even though the tor network is hosting on port 11009 and the client is attempting to connect to the same port